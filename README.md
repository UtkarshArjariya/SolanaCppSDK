# Solana C++ SDK

A C++ SDK for interacting with the Solana blockchain. Work in progress.

## Prerequisites
- C++17 compiler
- CMake >= 3.10
- libsodium

## Building
```bash
cmake -B build && cmake --build build
```
